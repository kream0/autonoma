{
  "name": "autonoma-desktop",
  "version": "0.1.0",
  "description": "Autonoma Desktop Application",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "dev": "electrobun dev",
    "build": "electrobun build",
    "build:mac": "electrobun build --platform darwin",
    "build:win": "electrobun build --platform win32",
    "build:linux": "electrobun build --platform linux"
  },
  "dependencies": {
    "electrobun": "^0.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
